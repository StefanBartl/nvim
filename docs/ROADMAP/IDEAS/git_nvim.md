# `git.nvim`

| Modul | Ziel-Plugin | Warum | Konfidenz |
|---|---|---|---|
| `config/lazygit/actions/{badd,replace}`, `resolve_path` | pickers / buffer-ctx / lib | Badd = Background-Buffer (dupliziert, s. Liste 2) | mittel |

+ alles git betreffenden externen plugins git betreffend: Anaylse ob es sich auszahlen würde, das was diese plugins anbieten, selbst in einene eigenen einzelenen plugin nachz8ubauen/zu mordernisieren, oder als altenative alle git plugins inn einen zsammenzufassen um diese zentral zu orchestrieren, wie ich das scho in `filetree.nvim`, `dap.nvim` usw... gemacht habe.

Analysiere das und schreibee in Konzept. Leitlinine dazu findest du in: `E:\repos\WKDBooks\Development\wkdbook-Lua\Checklists\NEW_PROJECT.md`

---

