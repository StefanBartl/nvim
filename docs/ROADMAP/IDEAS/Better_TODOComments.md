todo-comments.nvim ersetzen durch ein perfoprmanteres, umfangreicheres und sichereres plugin. Sicher im sinne von: es gabn bei todo-comments.nvim oft probleme, wenn ich custom einstellungen machte,custom definiert tags wurden statt ion dne korrekten files überall vhervorgehoben, auch zb in log files usw.. obwohl das tag nur ein teil es es wortes war, wie zb definiert DEBUG: und dann hat als tag ales wie debugging das debug usw. hervorgehebon. das war nicht zu korrigieren sondern nur per monkeypatch usw...

Jedefnalls soll ees alle features haebn die auch todo-comments.nvim hat + sinnvolle erweiterungern + kreuzfeatures zu meinen anderen nvim plugins.

name: `comments.nvim` oder `todo.nvim`
