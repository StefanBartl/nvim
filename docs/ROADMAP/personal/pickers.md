# `pickers.nvim`

Es wäre cool, wenn es möglich wäre mit zb.: `nvim **` nur mit lazy und pickers.nvim zu starten und dann noch mitzugeben als argument "snacks" und dann startet man einne nvim session ur imt lazy plugi manager und pickers.nvim der snacks verwendet, also snacks.nvim auch nstalliert. wen aber telescope verwendet weird, dann das... Mich würde generell interessieren was da möglich ist und in welchen ausmaßß wir die syntax steuern kölnnen, mit der man nvim sementpsrehnd startet damit da sicht zu kurz wuird oder hat man das gar keine chance und nvim gibt das klnallhart vor

---

