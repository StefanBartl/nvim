# Autocmds zentralisieren
* [ ] **Problem:** Viele Module nutzen direkt vim.api.nvim_create_autocmd, obwohl lib.autocmd existiert.
  * *Verbesserung:* Nach und nach auf lib.autocmd bzw. den vorhandenen FileType-Dispatcher migrieren (für einheitliche Fehlerbehandlung, Gruppen und zuverlässiges Reload-Verhalten).


Gleiche Methodik wie beim letzten Punkt. Zuerst schaue ich mir `lib.nvim.autocmd`s API an, dann einen breiten Grep über alle 26 Repos.

Now let's verify all 4 modules load and their autocmds actually register correctly.

All green. Let's commit lib.nvim's fix, then move through the remaining repos.

lib.nvim done. Now let's move through the remaining 13 repos with hits, smallest first.

`dap.nvim`, `diff.nvim` (×2), and `fileops.nvim` are already correctly migrated with the exact same reasoning I derived independently. `github_stats.nvim` even documents the same `buffer`-forwarding gap I just found in `color_my_ascii.nvim` — let me add the matching comment there.

Now let's check the remaining repos: `filetree.nvim`, `language.nvim`, `markdown.nvim`, `pickers.nvim`.

`filetree.nvim`'s `util/autocmd.lua` is a complete, well-designed wrapper matching exactly what I built for lib.nvim's exceptions — and it's the *only* place raw calls appear in that repo, meaning every caller already routes through it. Fully compliant, no action needed. Now markdown.nvim's 4 sites:

`markdown.nvim` is almost entirely already compliant — only `refs.lua`'s buffer-scoped autocmd needs the same documentation as the other exceptions.

Now the last two: `language.nvim` (6 hits) and `pickers.nvim` (5 hits).

Ausgeführt ein Befehl, gelesen eine Datei
Grepped language.nvim and pickers.nvim
cd /e/repos
echo "=== language.nvim ==="
grep -rn --include="*.lua" -E "vim\.api\.nvim_create_autocmd\(|vim\.api\.nvim_create_augroup\(" language.nvim/lua | grep -v "/TESTS/\|/tests/\|/test/\|_spec\.lua\|/debug/\|debug\.lua:\|/health\.lua"
echo; echo "=== pickers.nvim ==="
grep -rn --include="*.lua" -E "vim\.api\.nvim_create_autocmd\(|vim\.api\.nvim_create_augroup\(" pickers.nvim/lua | grep -v "/TESTS/\|/tests/\|/test/\|_spec\.lua\|/debug/\|debug\.lua:\|/health\.lua"
=== language.nvim ===
language.nvim/lua/language/bindings/autocmds/init.lua:15:  local group = vim.api.nvim_create_augroup("language_nvim", { clear = true })
language.nvim/lua/language/bindings/autocmds/init.lua:19:  vim.api.nvim_create_autocmd("BufDelete", {
language.nvim/lua/language/bindings/autocmds/init.lua:51:    vim.api.nvim_create_autocmd({ "BufWinEnter", "FileType" }, {
language.nvim/lua/language/bindings/autocmds/init.lua:62:    vim.api.nvim_create_autocmd({ "TextChanged", "InsertLeave" }, {
language.nvim/lua/language/bindings/autocmds/init.lua:70:      vim.api.nvim_create_autocmd("WinScrolled", {
language.nvim/lua/language/bindings/autocmds/init.lua:91:    vim.api.nvim_create_autocmd("BufWritePre", {

=== pickers.nvim ===
pickers.nvim/lua/pickers/bindings/autocmds.lua:34:    vim.api.nvim_create_autocmd("VimEnter", { once = true, callback = callback })
pickers.nvim/lua/pickers/selected_index/init.lua:155:  local aug = vim.api.nvim_create_augroup(augname, { clear = true })
pickers.nvim/lua/pickers/selected_index/init.lua:161:  vim.api.nvim_create_autocmd({ "CursorMoved" }, {
pickers.nvim/lua/pickers/selected_index/init.lua:171:  vim.api.nvim_create_autocmd({ "TextChangedI", "TextChanged" }, {
pickers.nvim/lua/pickers/selected_index/init.lua:177:  vim.api.nvim_create_autocmd("BufDelete", {
Lesen
init.lua
Nutzungslimit erreicht
Du hast dein Nutzungslimit erreicht. Versuche es erneut, nachdem dein Limit zurückgesetzt wurde.
