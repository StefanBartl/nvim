# Roadmap - `replacer.nvim`

- [ ] `replacer.nvim`
  - [ ] Bei scope `cwd` als beispoel kann es zu huunderten Treffern kommen, es wäre super, wenn man im picker drinnen ist, und die rearultsatslite hat, dass iman auf diese dann einfgach und übersichtlich filter anwednen kann. meine idfee dazu;
    - [ ] ein kaeymap öffnet ein "Fileter ui" - während diese offen isrt, kann im pciker nichts in der prompt gemacht ween oer durch die resaultatuslitte geagenagen wwerden. Dort kann man dann zb auswöhlen ob man
      - einen fiulter auf dateipfade setzte will, also zb check ja -> nebenbei ein kleiners textfeld, indem man den das wort/chars eingeben kan.
      - innerhalb der lste grep ausfphert, also wied checck ja -> textfeld indem man ein en begriff/char eibgeibt, übroig in der restuöatlsite bleiben dann nur jede files in denn da swrt vorkommt..
      - usw..
      - also im ewesbnetlichen eine neuewa filter window üner den picker, das aus tags und txtfelder besteht
    - [ ] odeer man fhrt das kemap saus, dann fragt es "Welchen filter wils du anwenden"  man wählt denn man will, dann prompt nach den begriff und dann word es angeweednet.

replacer-.nvimist hier eigentlich ein gutes Beispiel,das ganze könnte aber über pickers.nvim eingehöät für alle piker ein zusätzlicvhes filter ui feature sein, nciht wahr?
