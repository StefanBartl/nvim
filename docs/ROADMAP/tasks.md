# Tasks

## nvim-config

`CopyFileAbsolute...` -> Mit $REPOS_DIR

## All meine `.nvim`-Plugins

`Insert` sollte auch usrcmds meiner schwesterplugins kennen und anbieten, wiezb :Image paste um screesnhots aus der Zwischenablage einzufügen. Außerdem könnte man sagen, dass es auch zu `Markdown links` als eue option `paste` passen würde. Ales immer nur, wenn das plugin auch isntlaliert ist, also es wäre cool, wenn wir da so "Kreuzbindings" hätten die gut in andere plugins passen - diese aber nur shimmed werden, wenn das plugin auchibereits insgtalliert ist. nur wegenen usrcmds wird einanere plugin nicht isntlliert, damit ann es im esten okugin als anwenne kann. ich offe es ist klar, was ichgenau dmait meine.

## `ui.nvim`

- - marks
  -  -> ui, die aber nur zeigt nur files zeigt mit uncomitted changes
    - key mit dem man switchen kann zwischen uncomitted, staged, unstaged so dass die ui updatet wird.

## `:MyPlugins`

- `dashboard` soll auch die nvim-config zeigen
  - [option?] `fetch` impemntieren, damit gleich beim öffnen der fetch aller repos durchgeführt wird
  - [option?] `fetchThis` bzw `fetch .` implemeniteren, dass nur das repo gleich bem öffnen fetched, das
  - `reclone` usw... fragen dann ab: "Willst da xy wirklich tun?" -> wird momentan aber als `more` notify angezgit, bitte als selcetion/mit buttons aus der lib.nvim ui.kit


## `ui.nvim` & `:MyPlugins`

-> Diese`Workflows` sollten de facto fast bei allen usrcmds meiner nvim plugins implementiert sein. bitte schreibe das als task als eigene file in C:/Users/StefanBartl/AppData/Local/nvim/docs/ROADMAP/personal/All/FINISH/Final_Checks sowie die enthaltenen files auch, aber auch in meine [rules](C:/repos/WKDBooks/Development/wkdbook-Lua/Checklists/README.md) am passenden platz

## `pickers.nvim`

- - in der resultliste alle keymaps aus `filetree.nvim` implementieren, die sinn machen, wie `[a`, `ML`  usw...

---
