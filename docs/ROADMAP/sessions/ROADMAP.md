# Roadmap für das Sessions-Modul

## Misc

1. OpenInNvim new instance startet zwar eine neue instanz, aber ladet dann nicht das dokument. CWD ist aber korrekt! Und Linux version ist noch nicht implementiert.
2. Bei `SessionSave` schließt es den Neotree sofern offen. Auch bei anderen usercomands von 'session'?
3. Bessre Bezeichnung als "+SessionLoad"
4. Sol auch "+sessionload" oder "+sl" annehmen
5. wenn ein buffer nicht gespeichert ist, sollte er gepromptet werden, am besten aber nicht mit befelszeile sondern mit einer bufer ui.

---
