# ROADMAP markdown usrcmds.

## 1. Parser & CLI Erweiterung

* [ ] Glob Patterns (`*.md`, `*.lua`)
* [ ] Regex Filter (`--match`)
* [ ] Exclude Patterns (`--exclude "*.tmp"`)

---

## 2. Link Enhancements

* [ ] Relative Links (`--relative`)
* [ ] Git-root detection (`--git-root`)
* [ ] Anchor support (`file.md#section`)
* [ ] Line references (`file.md#L10`)

---

## 3. Neo-tree Integration

* [ ] Multi-select support (`ml` auf markierten Nodes)
* [ ] Batch operations auf Selection
* [ ] Preview Mode (links im Float anzeigen)
* [ ] Insert into current buffer at cursor

---

## 4. Markdown Tooling

* [ ] `:Markdown toc` → Table of contents generator
* [ ] `:Markdown rename-links` → sync renamed files
* [ ] `:Markdown validate-links` → broken link checker
* [ ] `:Markdown backlinks` → reverse graph

---

## 5. UX Improvements

* [ ] Telescope picker integration
* [ ] Live preview window
* [ ] Clipboard history integration
* [ ] Format presets (Obsidian / GitHub / Wiki)

---

## 6. Advanced Ideas

* [ ] Graph builder (Markdown → link graph)
* [ ] Link clustering by directory
* [ ] Smart auto-root detection per workspace
* [ ] “Wiki mode” navigation (graph traversal in Neo-tree style)

```
```

