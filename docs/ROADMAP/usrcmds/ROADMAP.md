# Roadmap for das `usrcmds`-Modul

## MDTablewrap

1. Es funktoinert nicht immer, obowhl es "table formatted" ausgiebt
2. Command, uma die Zelleninhalte zu zentrieren
3. Preview Funktion, die vielleicht MarkdownPreview, Preview mode von mdview oder image (aktualsieren?) benutzt
4. Modus, der Tabellen solange man in nvim ist auf width = equal bringt, wenn man den buffer verlässt aber wieder auf "normalen" umschreibt.
5. MDTablewrap solte nach `Custom` kommen, nicht in `usrcmds` (dafür ist es zu umfangreich)

---
