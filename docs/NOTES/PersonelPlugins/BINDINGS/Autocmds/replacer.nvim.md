# replacer.nvim — Autocmds Cheatsheet

**None.** Confirmed by explicit grep across the whole repo — no
`nvim_create_autocmd`/`nvim_create_augroup`/vimscript `autocmd` anywhere in
`lua/replacer/**`.

Cross-reference: `docs/BINDINGS.md:54` states this explicitly: *"None.
replacer.nvim registers no `autocmd`/`augroup` of any kind."* Matches source.
