--- C:\Users\Bernhard\AppData\Local\nvim\patches\todo_comments\old.lua	2025-12-18 01:34:12.000000000 +0100
+++ C:\Users\Bernhard\AppData\Local\nvim\patches\todo_comments\FIXED.lua	2025-12-18 01:29:46.000000000 +0100
@@ -1,36 +1,14 @@
--- FIX: dddddd
--- TODO: foobar
--- dddddd
 local Config = require("todo-comments.config")
 
 ---@module 'uv'
 
 local M = {}
 M.enabled = false
 M.bufs = {} ---@type table<number, boolean>
 M.wins = {} ---@type table<number, boolean>
 
--- PERF: fully optimised
--- FIX: ddddddasdasdasdasdasda
--- PERF: dddd
--- ddddd
--- dddddd
--- ddddddd
--- FIXME: dddddd
--- FIX: ddd
--- HACK: hmmm, this looks a bit funky
--- TODO: What else?
--- NOTE: adding a note
---
--- FIX: this needs fixing
--- WARNING: ???
--- FIX: ddddd
---       continuation
--- @TODO foobar
--- @hack foobar
 
 ---@type table<number, {valid: table<number, boolean>}>
 M.state = {}
 ---@type uv.uv_timer_t?
 M.timer = assert(vim.uv.new_timer())
 
@@ -88,12 +66,27 @@
 ---@param ns number
 ---@param hl string
 ---@param line number
 ---@param from number
 ---@param to number
 local function add_highlight(buf, ns, hl, line, from, to)
+  -- Defensive bounds checks: ensure from/to are within the actual line length.
+  local ok, text = pcall(vim.api.nvim_buf_get_lines, buf, line, line + 1, false)
+  if not ok or type(text) ~= "table" or text[1] == nil then
+    return
+  end
+  local line_text = text[1]
+  local max_col = #line_text
+  if type(from) ~= "number" or type(to) ~= "number" then
+    return
+  end
+  from = math.max(0, from)
+  to = math.min(max_col, to)
+  if from > to then
+    return
+  end
   vim.api.nvim_buf_set_extmark(buf, ns, line, from, {
     end_col = to,
     hl_group = hl,
     priority = 500,
   })
 end
