# md_tablewrap REAMDE

```lua
config = function()
require("usercmds.md_tablewrap").setup({
  inner_pad        = 1,
  outer_left       = 3,
  outer_right      = 3,
  auto_width       = true,
  max_col_width    = nil,
  min_col_width    = 20,
  wrap_all_default = false,
  on_save_enabled  = false,
})
```
