# config.neotree.keymaps.filesystem

Entry point that merges filesystem keymap modules into a single mapping
table. Only `files.lua` remains here — filter/save/replace/mark/navigation/
path/info/search were all migrated to filetree.nvim and deleted from this
config.
