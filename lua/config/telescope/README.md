# config.telescope

Modularized Telescope setup with file-browser keymaps. History,
preview-scroll (`<PageUp>`/`<PageDown>`) and history-nav (`<C-p>`/`<C-n>`)
are deliberately **not** configured here — they're owned by pickers.nvim,
which patches them globally.
