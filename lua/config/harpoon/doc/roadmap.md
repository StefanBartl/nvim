# roadmap WKD-`harpoon2`-Config

## Nächste sinnvolle Erweiterungen

- Optionaler Telescope-Picker mit identischer Label-Logik (eigener entry_maker).
- Konfigurierbare Öffnungs-Aktionen (same window/split/vsplit/tab) per Setup-Option.
- Optionale Persistenz-Debounce-Zeit (z. B. 150 ms) bei sehr häufigen Änderungen.

---

