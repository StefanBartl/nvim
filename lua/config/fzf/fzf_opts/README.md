# config.fzf.fzf_opts

Low-level fzf command-line options for fzf-lua. History is deliberately
**not** configured here — it's owned by pickers.nvim
(`history.fzf_scope = "patch"`), which patches fzf-lua's
`fzf_opts["--history"]` itself.
