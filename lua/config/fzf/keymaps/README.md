# config.fzf.keymaps

Keymaps for the fzf-lua prompt. Preview page-down/up (`<PageDown>`/
`<PageUp>`) are deliberately **not** bound here — they're owned by
pickers.nvim (`lua/pickers/keys/`), which patches them globally into
fzf-lua's `keymap.builtin`; see pickers.nvim's `docs/KEYMAPS.md`.
