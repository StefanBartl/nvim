# config.neotest.actions

Centralized Neotest actions (run, stop, debug, summary, ...) usable by
keymaps, user commands and menus — the single implementation every other
`config.neotest.*` integration (keymaps, commands, telescope, whichkey,
neotree) delegates to.
