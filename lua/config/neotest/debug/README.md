# config.neotest.debug

Neotest debug tooling: `:NeotestDebugAdapters`/`State`/`File`/`Root`/
`Framework` user commands, `M.keymaps()`, and `M.setup_all()` wiring both
up — for diagnosing why an adapter isn't finding tests in a file.
