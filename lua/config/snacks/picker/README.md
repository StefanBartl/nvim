# config.snacks.picker

Thin adapter that assembles `Snacks.picker`'s options from pickers.nvim,
delegating in-picker keys to it so the in-picker UX stays unified across
every picker engine (telescope / fzf-lua / snacks).
