# lsp.languages.webdev

**Not the active path.** This `M.enable_all()` is not called from anywhere
in this config; the parent `lsp.languages` module's own `enable_webdev()`
— a different, overlapping language list — is what actually runs.
