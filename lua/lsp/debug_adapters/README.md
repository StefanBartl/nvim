# lsp.debug_adapters

**Dead code.** Every `require` in this `init.lua` is commented out, and
nothing else in this config requires `lsp.debug_adapters` at all — the
`dotnet`/`go`/`node`/`webdev` siblings are unreachable through this entry
point.
