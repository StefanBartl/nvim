---@module 'wkdnvchad.config.statusline.normal'
--- Default NvChad statusline (no customization)

local M = {}

M.ui = {
  statusline = {
    order = nil, -- Use NvChad defaults
    modules = nil,
  },
}

return M
