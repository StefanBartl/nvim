# plugins.personal

Personal and local development plugins — the spec implementation only. All
source control (which repo loads locally / remotely / not at all, the
global override switch, machine-role handling, the per-repo mode table)
lives in `plugins.personal.source`; this module just registers the specs.
