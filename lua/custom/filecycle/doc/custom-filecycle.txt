*filecycle.txt*  Load previous/next file in current directory                     *filecycle*

Module:  custom.filecycle
Version: 0.2.0
For:     Neovim 0.9+ (0.10+ recommended)

==============================================================================
CONTENTS                                                          *filecycle-contents*

Introduction ......................................................... |filecycle|
Quick start .............................................. |filecycle-quickstart|
Installation (Lazy) ......................................... |filecycle-install|
Commands .................................................... |filecycle-commands|
Configuration ................................................ |filecycle-config|
API (Lua) ........................................................ |filecycle-api|
Mappings ..................................................... |filecycle-mappings|
Behavior and notes .............................................. |filecycle-notes|
Troubleshooting ........................................ |filecycle-troubleshoot|
FAQ .................................................................. |filecycle-faq|
License and notices ............................................. |filecycle-legal|

==============================================================================
INTRODUCTION                                                             *filecycle*

filecycle is a small module that navigates alphabetically to the next or
previous file in the directory of the current buffer. Navigation can optionally
wrap around at the end/beginning, include hidden files, and canonicalize
symbolic links to their real paths.

The open target is configurable:
• replace: replace current buffer content (default)
• stay: open in new buffer, keep old buffer, focus new
• split: horizontal split
• vsplit: vertical split
• tab: new tab
• background: load buffer only (no focus)

No external dependencies. Platform: Linux/macOS.

==============================================================================
QUICK START                                                     *filecycle-quickstart*

After setup:

>

:NextFile           " replace current buffer with next file
:PreviousFile       " replace current buffer with previous file
:NextFile!          " force switch without :confirm edit on modified buffers
:PreviousFile!
:NextFile stay      " open in new buffer (keep old buffer)
:PreviousFile stay
<
Recommended mappings:

>

nnoremap <leader>nf :NextFile<CR>        " replace buffer (default)
nnoremap <leader>pf :PreviousFile<CR>
nnoremap <leader>nfn :NextFile stay<CR>  " stay mode (new buffer)
nnoremap <leader>pfn :PreviousFile stay<CR>
<

==============================================================================
INSTALLATION (LAZY.NVIM)                                        *filecycle-install*

Example for a local module under nvim/custom/filecycle:

>

{
dir = vim.fn.stdpath("config") .. "/nvim/custom/filecycle",
name = "filecycle",
lazy = true,
config = function()
require("custom.filecycle").setup({
open_target = "replace",     -- "replace"|"current"|"split"|"vsplit"|"tab"|"background"
keep_focus = true,
include_hidden = false,
wrap = true,
follow_symlinks = true,
root = "buffer_dir",         -- "buffer_dir"|"cwd"
confirm_on_modified = true,  -- use :confirm edit for changed buffers
case_insensitive = true,
})
end,
keys = {
{ "<leader>nf", function() require("custom.filecycle").open("next") end,
desc = "[filecycle] Next file (replace)" },
{ "<leader>pf", function() require("custom.filecycle").open("prev") end,
desc = "[filecycle] Previous file (replace)" },
{ "<leader>nfn", function() require("custom.filecycle").open("next", {open_target="current"}) end,
desc = "[filecycle] Next file (stay)" },
{ "<leader>pfn", function() require("custom.filecycle").open("prev", {open_target="current"}) end,
desc = "[filecycle] Previous file (stay)" },
},
cmd = { "NextFile", "PreviousFile" },
}
<

==============================================================================
COMMANDS                                                      *filecycle-commands*

*:NextFile*                                         :NextFile  *:NextFile* *:NextFile!*
Open the next file (alphabetically) using the configured target (see
|filecycle-config|.open_target).

Arguments:
• %          : replace current buffer (default)
• stay       : open in new buffer, keep old buffer, focus new
• new/bg/background : load in background buffer
• split      : open in horizontal split
• vsplit     : open in vertical split
• tab        : open in new tab

With "!" the option confirm_on_modified is temporarily disabled.

*:PreviousFile*                               :PreviousFile  *:PreviousFile* *:PreviousFile!*
Open the previous file. With "!" confirm_on_modified is temporarily
disabled.

Arguments: same as :NextFile

Notes:
• If the current buffer has no filename, the operation aborts (no directory
can be derived).
• If the current file is not part of the filtered list (e.g. dotfile while
include_hidden=false), it is virtually inserted to compute stable
neighbors.

==============================================================================
CONFIGURATION                                                   *filecycle-config*

Setup function:

>

require("custom.filecycle").setup({ ... })
<
Options and defaults:

## Name                 Type                                   Default

open_target          "replace"|"current"|"split"|"vsplit"|
"tab"|"background"                    "replace"

keep_focus           boolean                               true
(only for "split"/"vsplit")

include_hidden       boolean                               false
(include dotfiles)

wrap                 boolean                               true
(wrap around at edges)

follow_symlinks      boolean                               true
(canonicalize via fs_realpath)

root                 "buffer_dir"|"cwd"                    "buffer_dir"
(base directory)

confirm_on_modified  boolean                               true
(:confirm edit for modified buffers)

case_insensitive     boolean                               true
(case-insensitive sort/match)

Details:
• open_target="replace": replaces current buffer content (default behavior)
• open_target="current": opens in new buffer, keeps old buffer in buffer
list, focuses new buffer (stay mode)
• open_target="background": loads the buffer without switching windows
(bufadd/bufload)
• keep_focus applies to splits only; focus is restored via vim.schedule()
• follow_symlinks=false compares "raw" paths; identical files reachable via
different link paths are then treated as different

==============================================================================
API (LUA)                                                         *filecycle-api*

*filecycle.setup()*

>

--- Initialize or override defaults.
--- @param cfg FileCycle.Config|nil
require("custom.filecycle").setup({
wrap = true,
case_insensitive = true,
})
<

*filecycle.open()*

>

--- Programmatic navigation with optional per-call overrides.
--- @param mode '"next"'|'"prev"'
--- @param overrides FileCycle.Config|nil
require("custom.filecycle").open("next")
require("custom.filecycle").open("prev", { open_target = "split", keep_focus = true })
<

Type reference (LuaLS/Emmy, expressed separately in code):

>

---@alias FilePath string
---@class FileCycle.Config
---@field open_target         "replace"|"current"|"split"|"vsplit"|"tab"|"background"
---@field keep_focus          boolean
---@field include_hidden      boolean
---@field wrap                boolean
---@field follow_symlinks     boolean
---@field root                "buffer_dir"|"cwd"
---@field confirm_on_modified boolean
---@field case_insensitive    boolean
<

==============================================================================
MAPPINGS                                                    *filecycle-mappings*

Example:

>

vim.keymap.set("n", "<leader>nf", function()
require("custom.filecycle").open("next")
end, { desc = "[filecycle] Next file (replace buffer)", silent = true })

vim.keymap.set("n", "<leader>pf", function()
require("custom.filecycle").open("prev")
end, { desc = "[filecycle] Previous file (replace buffer)", silent = true })

vim.keymap.set("n", "<leader>nfn", function()
require("custom.filecycle").open("next", { open_target = "current" })
end, { desc = "[filecycle] Next file (stay mode)", silent = true })

vim.keymap.set("n", "<leader>pfn", function()
require("custom.filecycle").open("prev", { open_target = "current" })
end, { desc = "[filecycle] Previous file (stay mode)", silent = true })
<

Default keymaps (when keymaps=true in setup):

| Keymap       | Action                                      |
| ------------ | ------------------------------------------- |
| <leader>nf   | Next file (replace buffer)                  |
| <leader>pf   | Previous file (replace buffer)              |
| <leader>nfn  | Next file (stay mode - new buffer)          |
| <leader>pfn  | Previous file (stay mode - new buffer)      |
| <leader>nF   | Next file (background buffer)               |
| <leader>pF   | Previous file (background buffer)           |
| <leader>NF   | Next file (vsplit)                          |
| <leader>PF   | Previous file (vsplit)                      |

==============================================================================
BEHAVIOR AND NOTES                                               *filecycle-notes*

• Base directory: root="buffer_dir" uses the current file's directory;
root="cwd" uses the current working directory.
• Listing: non-recursive via vim.fs.dir; type fallback via uv.fs_stat on
older libuv versions.
• Sorting: alphabetical, optionally case-insensitive.
• Boundaries: with wrap=false, a "boundary reached" message is shown at
the edges.
• Background loading: open_target="background" marks the buffer as listed
without window switches.
• Replace mode: open_target="replace" (default) replaces current buffer
content, respecting confirm_on_modified (hover_select dialog if modified).
• Stay mode: open_target="current" opens in new buffer, keeps old buffer
in buffer list, focuses new buffer.
• Safety: confirm_on_modified protects against discarding unsaved changes;
"!" on commands temporarily disables it.

==============================================================================
TROUBLESHOOTING                                            *filecycle-troubleshoot*

## Problem                           Cause / remedy

"no files in directory"           Filter excludes everything (e.g. only dotfiles
while include_hidden=false) or directory empty.

"current buffer has no file name" Unnamed buffer; save the file or use
root="cwd".

"boundary reached (wrap disabled)"
wrap=false and edge reached; enable wrap.

"order unexpected"                Check case_insensitive; when true, comparisons
use lowercase.

"same file appears twice"         follow_symlinks=false with multiple link paths;
set follow_symlinks=true.

==============================================================================
FAQ                                                                  *filecycle-faq*

Q: Why is the current file sometimes "inserted" although it would be filtered?
A: To compute stable neighbors, the current file is virtually inserted into
the sorted list if it would otherwise be missing (e.g. dotfile while
include_hidden=false).

Q: What's the difference between "replace" and "stay" mode?
A: "replace" (default) replaces the current buffer content (like :edit),
keeping only one buffer. "stay" mode opens the new file in a new buffer
while keeping the old buffer in the buffer list.

Q: Can I sort by extension or size?
A: Not currently; the module is intentionally minimal (alphabetical only).
A custom sort could be added inside list_files().

Q: Is Windows supported?
A: The module focuses on Linux/macOS by default. Windows is not actively
supported.

==============================================================================
Copyright (c) 2025.
vim:tw=78:ft=help:et:ts=8:sts=2:sw=2:
