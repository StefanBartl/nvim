# bindings.usrcmds.bindings_explorer

`:Bindings` — a picker over the hand-written BINDINGS cheatsheets
(`docs/NOTES/PersonelPlugins/BINDINGS` + `docs/NOTES/ExternPlugins/Bindings`).
See `docs/FEATURES.md` and `:help bindings_explorer` for the full feature
list, including the drift check against live keymaps/commands.
