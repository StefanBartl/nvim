# bindings.usrcmds.telemetry_nvim_config

`:RATelemetryNvimConfig` / `:RATelemetryNvimConfigFull` — flat aliases for
`:RATelemetry setup nvim-config` / `:RATelemetry full nvim-config`. Aliases
only; the actual mechanism lives entirely in runtime-analysis.nvim.
