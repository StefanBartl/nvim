# bindings.mappings.utils.window_zoom

Toggle-maximize the current window and restore the previous layout sizes
afterward, using `winrestcmd()` to capture/restore the full layout. State is
kept per tabpage so zooming in one tab never interferes with another.
