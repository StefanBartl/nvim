# lsp.debug_adapters.webdev.browser

Browser-based debugging for web apps via `js-debug-adapter`. Requires Chrome
to be started with remote debugging enabled — see this module's own
comments for the exact flags. Part of the unreachable `lsp.debug_adapters`
tree (see its README).
