# lsp.tools.eslint_prettier

Main entry for eslint/prettier tooling. Exposes `setup()` and `attach()`
(the latter registers autocmds/user commands). Works cross-platform and
resolves mason-installed binaries automatically.
