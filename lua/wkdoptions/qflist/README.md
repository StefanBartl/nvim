# wkdoptions.qflist

Diagnostic display config: virtual-text spacing/prefix, underline, signs,
severity-sorted — the same `vim.diagnostic.config()` surface
`lsp.core.diagnostics` also touches, worth checking both stay in sync.
