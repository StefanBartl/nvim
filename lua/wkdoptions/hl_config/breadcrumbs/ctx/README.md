# wkdoptions.hl_config.breadcrumbs.ctx

Breadcrumb context orchestrator: gets the node at the cursor (cached per
tick), then runs the configured provider pipeline (LSP, Treesitter,
language-specific) to build the crumb list.
