# wkdoptions.options_config

Editor option toggles and global behaviors that aren't purely visual
highlight groups: cursorline/column defaults (cooperating with
`hl_config`), guicursor mapping to custom highlight groups, and an optional
subtle matchparen blink (`enable_matchparen`, `matchtime_tenths`).
