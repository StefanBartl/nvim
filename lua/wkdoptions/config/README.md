# wkdoptions.config

Central, modular configuration with lazy loading and an observer pattern.
Reload with `:luafile %`/`:source %`; live updates via `:MyHlSet {key}
{value}` or `:MyOptSet {key} {value}`.
